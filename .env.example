# Copy to .env and fill in YOUR ivelt.com account. Stays on this machine only.
# The server logs in as you and only READS pages (it never posts).
IVELT_USERNAME=
IVELT_PASSWORD=

# Optional overrides (defaults are fine for ivelt.com):
# IVELT_BASE_URL=https://www.ivelt.com/forum
